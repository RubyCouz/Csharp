using System;

namespace exo2
{
    class Program
    {
        static void Main(string[] args)
        {
            bool isEasy = true;
            if (isEasy == true)
                Console.WriteLine("C'est facile!!");
            else
                Console.WriteLine("C'est difficile!!");
        }
    }
}
